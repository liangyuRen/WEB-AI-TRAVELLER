# 🔑 Supabase 密钥详解: Public vs Private

## 核心概念

你的理解完全正确！Supabase 有两个不同的 Key，用于不同的场景：

---

## 📊 密钥对比表

| 特性 | Public Key (公开) | Private Key (私有) |
|------|------------------|-------------------|
| **名称** | Anon/Public Key | Service Role Key |
| **权限级别** | 普通用户权限 | 完整管理权限 |
| **安全性** | 可以暴露于客户端 | ⚠️ 保密，不能公开 |
| **使用场景** | 前端应用 | 后端服务器 |
| **RLS 策略** | ✅ 受 RLS 限制 | ❌ 绕过 RLS |
| **可访问数据** | 仅自己的数据 | 所有数据 |

---

## 🔓 Public Key (前端使用)

### 什么是 Public Key？

```
REACT_APP_SUPABASE_ANON_KEY = "eyJhbGc..."
```

### 特点

✅ **可以在前端暴露** - 直接写入 `.env.local`
✅ **受 RLS 保护** - 用户只能访问自己的数据
✅ **权限受限** - 无法进行管理操作
✅ **用于认证** - 用户登录、注册

### 工作流程

```
用户在前端输入邮箱密码
    ↓
前端使用 Public Key 连接 Supabase
    ↓
Supabase 认证用户身份
    ↓
返回 JWT Token
    ↓
用户登录成功
    ↓
后续请求使用 JWT Token (不再需要 Public Key)
    ↓
RLS 策略自动检查:
  这个 Token 对应的用户是否有权限访问这个数据？
```

### 前端使用例子

```typescript
// frontend/src/services/supabase.ts

import { createClient } from '@supabase/supabase-js';

const supabaseUrl = process.env.REACT_APP_SUPABASE_URL;
const supabaseAnonKey = process.env.REACT_APP_SUPABASE_ANON_KEY; // Public Key

// 在浏览器中运行，所有人都能看到这个 Key（但由于 RLS，也没关系）
export const supabase = createClient(supabaseUrl, supabaseAnonKey);

// 用户注册
export const signUp = async (email, password, name) => {
  const { data, error } = await supabase.auth.signUp({
    email,
    password,
    options: {
      data: { display_name: name }
    }
  });
  return data;
};

// 用户登录
export const signIn = async (email, password) => {
  const { data, error } = await supabase.auth.signInWithPassword({
    email,
    password
  });
  return data;
};

// 获取用户数据（RLS 自动限制只能获取自己的）
export const getCurrentUser = async () => {
  const { data: { session } } = await supabase.auth.getSession();
  return session?.user;
};
```

---

## 🔐 Private Key (后端使用)

### 什么是 Private Key？

```
SUPABASE_KEY = "eyJhbGc..." (Service Role Key)
```

### 特点

❌ **不能在前端暴露** - 只在后端环境变量中
❌ **绕过 RLS** - 可以访问所有数据
✅ **权限完整** - 可以进行任何数据库操作
✅ **用于管理** - 后端管理操作

### 工作流程

```
后端代码需要操作数据库
    ↓
使用 Service Role Key 连接 Supabase
    ↓
Supabase 验证 Key 的权限
    ↓
允许访问任何数据（绕过 RLS）
    ↓
执行数据库操作（增删改查）
```

### 后端使用例子

```typescript
// backend/src/services/supabase.ts

import { createClient, SupabaseClient } from '@supabase/supabase-js';

let supabase: SupabaseClient | null = null;

export const initSupabase = (url?: string, key?: string): SupabaseClient => {
  const supabaseUrl = url || process.env.SUPABASE_URL;
  const supabaseKey = key || process.env.SUPABASE_KEY; // Private Key!

  if (!supabaseUrl || !supabaseKey) {
    throw new Error('Missing Supabase credentials');
  }

  // 只在后端使用，不会暴露给客户端
  supabase = createClient(supabaseUrl, supabaseKey);
  return supabase;
};

// 旅行计划服务（后端才能做的操作）
export const travelPlanService = {
  // 创建行程
  async create(userId: string, data: any) {
    const sb = getSupabase();
    const { data: result, error } = await sb
      .from('travel_plans')
      .insert([{
        user_id: userId,
        destination: data.destination,
        budget: data.budget,
        // ... 其他字段
      }])
      .select();

    if (error) throw error;
    return result?.[0];
  },

  // 获取所有用户的行程（后端特权操作）
  async getAllPlans() {
    const sb = getSupabase();
    const { data, error } = await sb
      .from('travel_plans')
      .select('*'); // 可以获取所有行程，不受 RLS 限制

    if (error) throw error;
    return data || [];
  },

  // 删除行程（后端特权操作）
  async delete(planId: string) {
    const sb = getSupabase();
    const { error } = await sb
      .from('travel_plans')
      .delete()
      .eq('id', planId);

    if (error) throw error;
  }
};
```

---

## 🛡️ RLS 与两个 Key 的关系

### 用 Public Key (前端)

```typescript
// 前端代码
const { data } = await supabase
  .from('travel_plans')
  .select('*')
  .eq('user_id', userId);

// RLS 检查:
// 1. 用户已登录吗？
// 2. 这个 user_id 等于当前登录用户吗？
// 3. 如果是，返回数据；如果否，返回错误

// 结果: 用户只能看到自己的行程
```

### 用 Private Key (后端)

```typescript
// 后端代码
const { data } = await supabase
  .from('travel_plans')
  .select('*');
  // 没有添加任何 where 条件

// RLS 检查:
// 绕过所有 RLS 策略（因为使用了 Service Role Key）

// 结果: 后端可以看到所有用户的行程
```

---

## 💡 实际工作流程示例

### 场景: 用户想生成旅行计划

```
┌─────────────────────────────────────────────────────┐
│ 步骤 1: 用户在前端输入信息                          │
│ destination: "日本", days: 5, budget: 10000         │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 步骤 2: 前端发送 HTTP 请求到后端                    │
│ POST /api/travel/plan                               │
│ (使用 Public Key 认证，获取 JWT Token)             │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 步骤 3: 后端接收请求                                │
│ - 验证 JWT Token 有效性                            │
│ - 提取用户 ID (userId = "xxx")                     │
│ - 调用 LLM 生成行程                                │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 步骤 4: 后端将行程保存到数据库                      │
│ 使用 Private Key (Service Role Key)                │
│ - 绕过 RLS，直接写入 travel_plans 表               │
│ - 自动设置 user_id = userId (步骤 3 提取的)       │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 步骤 5: 后端返回结果给前端                          │
│ { success: true, data: {...} }                      │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 步骤 6: 前端显示行程给用户                          │
│ (后端返回的数据已经限制为当前用户的)               │
└─────────────────────────────────────────────────────┘
```

---

## 🔄 数据安全流程

### 前端如何只看到自己的数据

```typescript
// 前端请求
const { data } = await supabase
  .from('travel_plans')
  .select('*');

// 虽然没有 .eq('user_id', userId)，
// 但 RLS 策略会自动添加这个过滤

// RLS 策略:
CREATE POLICY "Users can view their own travel plans"
  ON travel_plans
  FOR SELECT
  USING (auth.uid() = user_id);

// auth.uid() 会自动获取当前登录用户的 ID
// 所以结果自动限制为当前用户的数据
```

### 后端可以看到所有数据

```typescript
// 后端请求（使用 Service Role Key）
const { data } = await supabase
  .from('travel_plans')
  .select('*'); // 没有任何过滤

// 结果: 返回所有用户的所有行程
// （RLS 不适用，因为使用了 Service Role Key）
```

---

## ⚠️ 安全最佳实践

### ✅ 做这些

```
✅ 前端: 使用 Public Key (ANON_KEY)
  - 可以写在 .env.local
  - 可以暴露于浏览器
  - 由 RLS 保护

✅ 后端: 使用 Private Key (SERVICE_ROLE_KEY)
  - 必须保存在 .env 文件
  - 不能提交到 Git
  - 不能暴露于前端

✅ 后端验证: 检查请求的 JWT Token
  - 从 Token 中提取 user_id
  - 确保用户只能操作自己的数据

✅ 使用 RLS: 数据库层面的防护
  - 即使私钥泄露，用户也不能看到别人数据
  - 自动限制用户权限
```

### ❌ 不要做这些

```
❌ 前端: 不要使用 Service Role Key
  - 会暴露在浏览器中
  - 用户可以绕过 RLS，访问所有数据
  - 严重安全风险！

❌ 后端: 不要硬编码 Private Key
  - 如果泄露，攻击者可以访问所有数据
  - 必须使用环境变量

❌ 任何地方: 不要在日志中输出密钥
  - 日志可能被第三方访问
  - 使用日志时隐藏敏感信息

❌ Git: 不要提交 .env 文件
  - .env 已经在 .gitignore 中
  - 如果意外提交，立即删除并轮换密钥
```

---

## 🔐 现在的项目配置

### 你的项目配置完全正确！

```
┌─ 前端 (frontend/.env.local) ─────────────────┐
│                                              │
│ REACT_APP_SUPABASE_ANON_KEY = "public..."  │
│ ✅ 这是 Public Key                          │
│ ✅ 可以在浏览器暴露                          │
│ ✅ 由 RLS 保护                              │
│                                              │
└──────────────────────────────────────────────┘

┌─ 后端 (backend/.env) ────────────────────────┐
│                                              │
│ SUPABASE_KEY = "service_role..."           │
│ ✅ 这是 Private Key (Service Role Key)     │
│ ✅ 只在后端服务器中使用                      │
│ ✅ 受 .env 和 .gitignore 保护               │
│                                              │
└──────────────────────────────────────────────┘
```

---

## 📝 总结

| 方面 | Public Key | Private Key |
|------|-----------|------------|
| **是什么** | Anon/Public Key | Service Role Key |
| **谁用** | 前端 (浏览器) | 后端 (服务器) |
| **权限** | 普通用户权限 | 完整管理权限 |
| **安全** | 可以公开 | 必须保密 |
| **放哪** | frontend/.env.local | backend/.env |
| **RLS** | ✅ 被 RLS 限制 | ❌ 绕过 RLS |
| **例子** | `eyJhbGc...` (anon) | `eyJhbGc...` (service_role) |

---

**版本**: 1.0.0
**理解程度**: 你已经完全掌握了这个核心概念！✅

